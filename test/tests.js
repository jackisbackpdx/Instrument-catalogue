const test = QUnit.test;

test('time to test!', function(assert) {
    //Arrange
    // Set up your parameters and expectations
    const 
    const expected = <li class="drums" title='Get loud and throw down with this drum set, so dope!'>
    <h3>Drum Set</h3><img src='../assets/images/drumkit.jpeg'><p class="price">$200<button>Buy</button></p></li>

    result = 
    
    //Act 
    // Call the function you're testing and set the result to a const

    //Assert
    // What should the result be?

    assert.equal(true, false);
});